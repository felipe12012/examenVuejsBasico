<template>
    <div>
        <!-- <h1>{{cont.nombreProducto+" "+ cont.modelo}}</h1> -->
        <div
            class="card"
            style="width: 18rem"
            v-for="cont in contenedor"
            :key="cont.id"
        >
            <img :src="cont.url" class="card-img-top" :alt="cont.modelo" />
            <div class="card-body">
                <h5 class="card-title">{{ cont.nombreProducto + " " + cont.modelo }}</h5>
                <p class="card-text">
                    Some quick example text to build on the card title and make
                    up the bulk of the card's content.
                </p>
                <button  type="button" href="#" :id="cont.id" @click="addCarrito({id:cont.id, data:contenedor})" class="btn btn-primary col-12">Agregar</button>
                
            </div>
            
        </div>
    </div>
</template>

<script>
import {mapMutations,mapState }from 'vuex'

export default {
    name: "card",
    props: ["contenedor"],
    computed:{
        ...mapState(['carrito'])
    },
    methods:{
        
        ...mapMutations(['addCarrito'])
        
    }
};
</script>
